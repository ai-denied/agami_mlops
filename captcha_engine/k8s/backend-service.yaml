apiVersion: v1
kind: Service
metadata:
  name: captcha-api
  labels:
    app: captcha-api
spec:
  type: ClusterIP
  selector:
    app: captcha-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
