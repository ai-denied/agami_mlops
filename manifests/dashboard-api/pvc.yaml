apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dashboard-cache-pvc
  namespace: agami
  labels:
    app: dashboard-api
    component: cache-store
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
