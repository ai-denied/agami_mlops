# 운영 빌드 (npm run build) 가 자동 인식.
# 같은 호스트의 /captcha 로 라우팅 → Bastion Nginx Ingress 가 captcha-api 로 전달.
# (기존 /api 는 agami-ingress 의 카카오 로그인 백엔드와 충돌하므로 /captcha 로 분리.)
# 상대 경로이므로 HTTPS 전환되어도 변경 불필요.
VITE_API_URL=/captcha
VITE_CAPTCHA_CLIENT_KEY=ck_test
