# =============================================================================
# flashlight training 이미지 의존성
#
# 용도: train_mouse_gru.py 실행 전용 컨테이너 (Dockerfile.training)
# pipeline 이미지(requirements-flashlight.txt)와 별도로 관리한다.
#
# 학습 단계를 pipeline 단계와 분리하는 이유:
#   - torch 포함 시 이미지 크기가 ~1GB 이상으로 증가
#   - package/compare/promote 단계는 torch 불필요
#   - GPU/CPU 환경에 따라 torch 버전을 별도로 관리하기 용이
# =============================================================================

# 수치 연산
numpy>=1.24.0
pandas>=2.0.0

# 모델 학습 · 직렬화
joblib>=1.3.0
scikit-learn>=1.3.0

# ONNX 내보내기 (train_mouse_gru.py 내부에서 torch.onnx.export 사용)
onnx>=1.14.0
onnxruntime>=1.15.0
onnxscript>=0.1.0

matplotlib>=3.7.0

# PyTorch (CPU 전용 빌드 — GPU 환경은 Dockerfile에서 index-url을 교체)
# torch>=2.0.0 는 Dockerfile에서 --index-url 로 별도 설치
