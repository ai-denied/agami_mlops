# facial_recognition MLOps 파이프라인(promote_model.py/run_model_update_pipeline.py/
# smoke_test_model.py) 실행에 필요한 의존성. requirements-face-api.txt(추론 서버
# 전용)와는 별도 파일 - 이 파이프라인은 아직 어느 Docker 이미지에도 연결되어
# 있지 않고(전용 Dockerfile 없음), GPU 서버 등에서 직접 실행된다.

pyyaml>=6.0
numpy>=1.24.0

# evaluation/onnx_contract_check.py / promote_model.py의 ONNX 입출력 contract
# 검증 및 더미 추론에 필요.
onnxruntime>=1.15.0

# seq_scaler.joblib 파일 복사/검증 시 역직렬화에 필요 (scikit-learn StandardScaler)
joblib>=1.3.0
scikit-learn>=1.3.0

# 참고: kubectl은 pip로 설치되는 패키지가 아니다. run_model_update_pipeline.py의
# restart-inference-api 단계(--skip-restart 미지정 시)는 PATH에 kubectl 바이너리와
# 클러스터 접근 권한(kubeconfig)이 있어야 동작한다.
