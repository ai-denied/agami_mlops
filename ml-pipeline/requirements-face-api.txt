# =============================================================================
# face-liveness-api 추론 서버 의존성
#
# 포함: FastAPI + onnxruntime (추론 전용)
# 미포함: torch, mediapipe — 피처는 클라이언트가 추출, API는 ONNX만 실행
# =============================================================================

# 수치 연산
numpy>=1.24.0

# ONNX 추론
onnxruntime>=1.15.0

# scikit-learn: seq_scaler.joblib 역직렬화 (StandardScaler 클래스 참조)
scikit-learn>=1.3.0
joblib>=1.3.0

# API 서버
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
